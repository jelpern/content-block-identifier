README

Elements with multiple classes (e.g. <div class="class1 class2">) are entered into the Counter with key "class1.class2" which is proper CSS. Note that it is assumed that the order of the classes is the same in every element with the same combination of classes.